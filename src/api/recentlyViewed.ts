/**
 * @deprecated Accommodation Detail still consumes this compatibility name.
 * The feature-owned adapter is the only implementation and U9 removes this
 * facade with the legacy detail hook.
 */
export { recentlyViewedApi } from "../features/wishlist/api";
