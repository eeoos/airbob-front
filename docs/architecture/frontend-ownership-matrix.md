# Frontend Ownership and Cutover Matrix

> Operational registry for the migration described by the active plan.  
> Baseline state: U4 commit `f5222d5`, followed by U6, U5, U19, U7, U8, U9, and U10 cutovers.
> Current cutover state: app Router/session/structural UI plus feature-owned Auth, Wishlist, Search, Accommodation Detail, reservation-create, Review, and booking-payment slices are active.
> Current architecture meaning comes from
> [`current-frontend-architecture.md`](./current-frontend-architecture.md).

This file is deliberately mutable. Each implementation unit updates the rows it
owns in the same change that switches a production consumer. It records runtime
ownership; it is not a substitute for git history or plan progress.

## Registry rules

- **Active** is the only production route entry or mutation writer at this
  revision.
- **Compatibility reader** can read old data or project an owned result into a
  derived cache, but cannot perform a second backend/domain mutation.
- **Rollback** names the reader or artifact that makes a revert possible. It is
  never permission to mount old and new writers together.
- **Removal condition** must be satisfied in the named unit before a legacy
  surface is deleted.
- `none` means no compatibility layer exists or is required.

## Core architecture slice registry

The detailed route and workflow tables below refine these seven required owner
classes. No category may be inferred from a historical plan.

| Slice | Current/old owner | Target/new owner | Current active owner after U10 | Read compatibility | Rollback reader/artifact | Removal condition |
| --- | --- | --- | --- | --- | --- | --- |
| Route manifest | rollback-only `src/routes/routeDefinitions.ts`/matching/MainLayout; active legacy navigation helpers in `src/routes/paths.ts`; former `Router.tsx`/`routeConfig.tsx` deleted | `src/app/router/**` | App definitions + manifest + 15 lazy adapters; ten bodies are app/screen-owned | Five adapters read one exact assigned legacy route body; legacy consumers still use the parity-checked path copy | Remaining old metadata/MainLayout chain | Per-route body switch in U12-U13/U21; old manifest/path aliases removed U22 |
| URL/query | builders in `src/routes/routeQueryContracts.ts`; parsers and mirrored state in unmigrated feature routes/libs | app codecs under `src/app/router/codecs` and target screens | App codecs own Login return, Wishlist, Search, accommodation booking, review result, Payment Success callback, and Payment Fail reason; legacy parsers remain only for unswitched bodies | The payment-success boundary sits outside authentication, requires and replace-scrubs the Toss tuple from native and Router history, and passes it in memory only to the success adapter. The booking-payment workflow then claims it into exact owned storage. Fail URLs retain only a typed reason | Existing unswitched builders/parsers plus captured deep-link corpus | Profile mirrors are removed U13 and final aliases U22 |
| Query/cache | Former process client and session cleanup helpers under `src/query/**`; unmigrated feature `queryKeys.ts`/hooks and `publicCache.ts` remain | session-scoped Query option factories and feature repositories | `SessionProvider` owns the QueryClient generation; Wishlist, Search, Accommodation Detail, coupons, and Reviews own subject/epoch keys/meta and scoped projections; booking-payment uses command-local captured scopes | The physical generation bridge isolates unmigrated hooks; app composition joins owned projections only | `QueryProvider.tsx` and `sessionCacheBoundary.ts` are production-unreachable rollback/test compatibility | U12-U13 add remaining feature scopes; rollback Query/public seams leave U22 |
| Domain mutation | unmigrated feature hooks/routes and global API wrappers; old Wishlist/Detail/Review/payment writers deleted | explicit `src/workflows/**` commands plus feature ports | Wishlist membership, reservation create, review submission, checkout request, and payment confirmation/reconciliation each have one workflow writer | Owned results may invalidate legacy reservation reads, but no adapter repeats a server mutation | Existing unswitched route/hook behind unchanged API contract; immutable U10 build for the gateway rollback | Switch remaining editor/profile/host workflows in U12/U13; old writer deleted in its switching unit |
| Browser storage | Deleted reservation checkout/index and payment-attempt writers; dedicated legacy storage adapter is read/cleanup-only | platform versioned storage plus owned booking-payment repositories | `src/workflows/booking-payment/checkout/**` owns static subject-owned checkout and callback slots, exact allowlists, 60/15-minute TTLs, invalid-record purge, terminal/session cleanup, and one-way verified migration | Exact pre-U10 checkout/index may be read only for authenticated server-verified migration; a consumed old confirmed marker is only a reconciliation hint | Immutable pre-U10 build for an atomic revert; immutable U10 repository/schema for U11 rollback | U10 complete. No legacy writer or raw generic compatibility export remains; dedicated legacy input cleanup leaves only when rollback/migration support is retired |
| Shell/overlay | rollback-only MainLayout plus active AppHeader and legacy route-local overlay placement | `src/app/shells/**`, `src/app/overlays/**`, and the shared overlay bridge | Five app shells own the sole active `main`; `PageShell` is a role-locked nested section; one portal root hosts Dialog/Toast while OverlayProvider owns an ordered Dialog stack, focus restoration, topmost dismissal, and nested body lock | Dialog/Toast interaction state remains with its owning route/component while remaining custom overlay renderers move; existing public props are unchanged | Old MainLayout/main-bare mapping and Dialog inline fallback for isolated compatibility tests | U14 moves every remaining modal/popover/drawer renderer to the structural runtime; old layout/fallback seams are removed U22 |
| Screen | remaining feature route containers mixing orchestration and presentation | `src/screens/**` props-only screens with app route controllers | Auth, Wishlist, Search, Accommodation Detail, Reservation Confirm, Review, and Payment Result use props-only screens; five routes still bridge assigned legacy bodies | Compatibility adapters inject data/commands only and never add a second workflow writer | Assigned unswitched body and old metadata | Per-route characterization passes and controller/screen switches; remaining old bodies deleted in U12-U13/U21 |

## Route ownership

All fifteen app route adapters are the active lazy entries. Five pass typed
Router inputs into exactly one assigned legacy body. Login, Signup, Wishlist,
Search, Accommodation Detail, Reservation Confirm, Review, Payment Success,
Payment Fail, and NotFound are app/screen-owned. `AuthContext` remains a thin active adapter
only while its remaining legacy consumers move.

| Route ID | Current/old owner | Target/new owner | Active after U10 | Compatibility reader | Rollback before target cutover | Removal condition |
| --- | --- | --- | --- | --- | --- | --- |
| home | `src/features/home/HomeRoute.tsx` | `src/app/router/routes/HomeRoute.tsx` + `src/screens/home/HomeScreen.tsx` | app adapter → current/old body | none | Old manifest entry and current body | Target direct-load/lazy parity; remove old in U21 |
| search | deleted `src/features/search/SearchRoute.tsx`, route controller, and result hooks | app Search route + `src/screens/search/**` | **Active:** app codec/navigation/auth composition → `SearchController`/`SearchScreen` | App composition projects Wishlist membership into Search-owned, session-scoped query caches; `keepPreviousData` remains presentation-only placeholder data | Immutable pre-U8 build/commit; rollback is an atomic Git revert, never a live old body | Complete U8; retain the app codec, feature query/cache projection, and injected ports as the sole owners |
| accommodation-detail | deleted legacy route, booking/coupon/detail/review hooks, and global review/recently-viewed facades | app detail route + `src/screens/accommodation-detail/**` | **Active:** app codec/session/auth composition → `AccommodationDetailController`/`AccommodationDetailScreen`; reviews advance one cursor per visible load-more sentinel event | Owned wishlist projection updates only matching scoped Detail caches, while anonymous Detail projections force membership false; reservation-create commits the new owned checkout document and only a minimal history handle | Immutable pre-U9 build/commit; rollback is an atomic Git revert, never a live old body | Complete U9/U10 integration; retain owned API/models/query/cache/workflow boundaries |
| accommodation-confirm | deleted legacy route/modal/payment hooks/helpers | `src/app/router/routes/ReservationConfirmRoute.tsx` + `src/screens/reservation-confirm/**` | **Active:** app session/route/repository composition → `ReservationConfirmController`/`ReservationConfirmScreen` → sole payment-request workflow → official npm v2 gateway | Server-verified one-way read of an exact legacy checkout document; migrated input drops name/email and deletes its source | Immutable U10 commit `408d303` and its commit-specific Vercel deployment | U10 complete; U11 source cutover is green and awaits Vercel Preview sandbox parity before merge |
| accommodation-edit | `src/features/accommodations/edit/AccommodationEditRoute.tsx` | app edit route + `src/screens/accommodation-edit/**` | app adapter → current/old body | App path helper validates draft provenance; server detail remains authority | Old manifest entry | Editor transition/order/stale tests green; remove old in U12 |
| wishlist | deleted route/state hooks and global facades | app Wishlist route + `src/screens/wishlist/**` | **Active:** app codec → `WishlistController`/`WishlistScreen` | Search and Detail receive only their owned scoped cache projections. The Wishlist URL has no local mirror | Immutable pre-U7 build/commit; rollback is an atomic Git revert, never a live old writer | Complete U7-U9 integration; no old writer or projection adapter survives |
| profile | `src/features/profile/ProfileRoute.tsx` | app Profile route + `src/screens/profile/**` | app adapter → current/old body | Existing URL query decoded by new codec during switch | Old manifest entry | Guest/host URL and panel parity; remove old in U13 |
| host-reservation-detail | `src/features/reservations/HostReservationDetailRoute.tsx` | app host-detail route + reservation-detail screen package | app adapter → current/old body | none | Old manifest entry | Host detail terminals and API mapper parity; remove old in U13 |
| reservation-detail | `src/features/reservations/ReservationDetailRoute.tsx` | app reservation-detail route + `src/screens/reservation-detail/**` | app adapter → current/old body | App validates and consumes the typed review partial-success result once, then maps it to legacy display copy | Old manifest entry | Detail, cancel/review navigation and cache parity; remove old in U13 |
| reservation-review | deleted legacy route/hooks/global API and DTO | app Review route + `src/screens/review-create/**` | **Active:** app route/session/cache composition → `ReviewCreateController`/`ReviewCreateScreen` | Typed one-shot partial-success result only | Immutable pre-U9 build/commit; rollback is an atomic Git revert, never a live old body | Complete U9; retain owned review API/query/cache/workflow boundaries |
| payment-success | deleted legacy success route, confirmation hook, attempt registry, status hook, and global payment API | stable app-provider payment credential boundary + app success route + `src/screens/payment-result/**` | **Active:** pre-session callback parse/scrub and route-memory claim → authenticated booking-payment claim/replay bootstrap → `PaymentResultController` → sole confirmation/reconciliation workflow | Session bootstrap, keyed QueryClient replacement, and `RequireAuth` never receive or erase the memory-only claim before use. They never receive a credential-bearing search/history state. A consumed legacy marker is persisted as reconciliation-only. A fresh replay without browser documents must match the authenticated server-owned tuple and retains an in-place recheck surface | Immutable pre-U10 build/commit; immutable U10 schema/workflow for U11 rollback | Complete U10; only backend confirm or exact payment status publishes success |
| payment-fail | deleted legacy fail route, confirmation/status hooks, and distributed payment route-state helper | app fail route + `src/screens/payment-result/**` | **Active:** typed reason plus owned retry documents → `PaymentResultController` → reconciliation workflow | `confirm-failed` may reopen matching owned checkout/callback records; other reasons are display-only and clear payment browser state | Immutable pre-U10 build/commit; immutable U10 schema/workflow for U11 rollback | Complete U10; retry/pending preserves owned documents, invalid/terminal clears them |
| login | deleted `src/features/auth/LoginRoute.tsx` | `src/app/router/routes/LoginRoute.tsx` + `src/screens/auth/**` | **Active:** app safe-return codec/session command → shared auth controller/screen | Structured internal return target only | Immutable pre-U7 build/commit; rollback is an atomic Git revert, never a live old body | Complete U7; `RequireAuth`/`AuthContext` compatibility leaves with final consumers U22 |
| signup | deleted `src/features/auth/SignupRoute.tsx` | `src/app/router/routes/SignupRoute.tsx` + `src/screens/auth/**` | **Active:** feature signup command → shared auth controller/screen | none | Immutable pre-U7 build/commit; rollback is an atomic Git revert, never a live old body | Complete U7; global compatibility facade cleanup continues U22 |
| not-found | `src/routes/NotFoundRoute.tsx` | app NotFound route + `src/screens/not-found/**` | app-owned adapter/body | none | Old manifest entry/body | Bare-shell/direct-route parity; remove old in U21 |

## Mutable workflow and state ownership

| Capability | Current/old owner and writer | Target/new owner | Current active owner after U10 | Allowed read compatibility | Rollback/removal condition |
| --- | --- | --- | --- | --- | --- |
| Auth bootstrap/login/logout/401 | Deleted `useSessionQuery.ts`/`sessionLifecycle.ts`; deleted legacy auth route bodies/global auth API | `src/app/session/**` with feature auth commands and platform session signals | `SessionProvider` owns explicit states, operation/epoch stale guards, physical login/logout ordering, `/me` cancellation, focus/401 recovery, revocation, and cross-tab invalidation; owned Login/Signup screens consume injected commands | `AuthContext` projects booleans and delegates login/logout/revalidation only for remaining legacy consumers | Auth route cutover complete U7; `AuthContext` and `src/utils/authEvents.ts` facade leave after final consumers U22 |
| Auth-gated domain continuation | Legacy Search/Accommodation bodies previously retained component callbacks across `login()` | `src/workflows/auth-intent/**` plus app claim adapters and data-only execution ports | A stable memory-only provider owns one latest attempt outside the keyed Query subtree. Search and Accommodation adapters register/claim immutable primitive intents with captured subject/epoch | none | Complete U9. No second workflow writer or persistent intent may be introduced |
| Protected return target | `RequireAuth` writes structured React Router state; legacy auth parser/body deleted | app internal-return codec and Login adapter | App Login adapter validates the structured pathname/search/hash and navigates only after the owned controller reports success. The payment-success credential boundary scrubs and synchronizes the route before `RequireAuth` renders, so callback credentials can never enter this state | Current structured pathname/search/hash, excluding payment callback credentials | Route cutover complete U7; writer compatibility leaves with `RequireAuth` U22 |
| Query session cleanup | `src/query/QueryProvider.tsx` and `sessionCacheBoundary.ts` are retained rollback/test compatibility; the old manual root registry has no production owner | App session subject/epoch and scoped Query options | U5 generation-scoped QueryClient replacement plus keyed authenticated remount; Wishlist, Search, Accommodation Detail/coupons, and Reviews add subject/epoch key/meta and scoped publication/projection. U10 payment reads are command-scoped, abortable, and session-fenced rather than Query-cached | Unmigrated hooks run inside the physical generation bridge; this does not make their keys final scoped keys | Remaining viewer-dependent keys/meta and commands move U12-U13; compatibility Query files leave U22 |
| Session-owned browser cleanup | Deleted reservation cleanup port and legacy in-memory registry | App composition injects the booking-payment aggregate cleanup plus current-route cleanup into the session owner | `clearIdentityOwnedFrontendState.ts` clears the exact owned checkout/callback namespace and pre-U10 checkout/index/marker prefixes, retries one partial/storage-failed pass per namespace, then replaces a currently visible confirm/success/fail entry. A final non-cleared result fails the session transition closed before another identity is published | Back-stack entries cannot be enumerated; every checkout/callback reader still validates stable subject, epoch, purpose/version, TTL, exact fields, route/resource tuple, and operation | Complete U10. Cleanup is defense in depth; browser data never authorizes a payment command |
| Wishlist mutation | deleted mutation hooks, global API/DTO, compatibility app-shells, `wishlistCacheSync.ts`, and `publicCache.ts` | `src/workflows/wishlist-membership/**` plus feature API/reconciliation ports | **Active:** Search/Detail/Wishlist lazy adapters mount the sole route-scoped, subject/epoch-fenced command runner inside the current QueryClient generation. It owns Wishlist create/add/remove/delete/memo plus recently-viewed removal, exact-target single-flight, per-accommodation ordered lanes, abort/dispose, and server-reconciled create-only retry before any repeated add; route departure disposes the writer | App composition sends reconciled membership to Search and Detail owned projections; injected modal ports invoke the same writer and close performs no network request | Complete U7-U9; no old writer, legacy Detail projection, or global recently-viewed facade survives |
| Search request/navigation | deleted `useSearchResults.ts`, `useSearchRouteController.ts`, legacy query keys, and route body | Search controller, app URL codec, feature Query options, and narrow interaction reducer | **Active:** app adapter owns parse/push/replace/detail URL/auth composition; controller derives request/result/map state; TanStack Query owns result/loading/error/cancellation; SearchBar receives a typed route port | None; `keepPreviousData` is a Query placeholder snapshot, not a second state owner | Complete U8; old body, Search app-shell, global search API/DTO method, and global Maps/Places facades are deleted |
| Reservation create/handoff | deleted `useAccommodationBooking.ts` and `reservationCheckoutState.ts` | `src/workflows/booking-payment/reservation-create/**` plus the checkout/callback repositories | **Active:** one validated, same-Promise, route/session-fenced reservation writer preflights the single active payment pair before POST and rechecks before commit, then writes one subject-owned checkout document and passes only purpose/version/operation ID in history. Exact active recovery redirects before a second reservation POST; unreadable conflict fails closed. An interrupted possibly-sent reservation command remains uncertain and cannot be blindly repeated | No legacy writer remains; exact pre-U10 data is read only by the confirm-route migration path | Complete U9/U10; the new repository commit is the only handoff writer |
| Checkout request | deleted legacy confirm route, ReservationModal, `useReservationPayment`, and Toss feature helper | booking-payment checkout repository/request workflow using `PaymentGatewayPort` | **Active:** the confirm app adapter reads the owned checkout, consumes its history handle, verifies that no joined callback exists, derives current customer fields in memory, and the route/session-fenced workflow makes one npm v2 gateway request. The official adapter maps `CARD`/`KRW` and `ANONYMOUS` without changing redirect or server contracts. Any persisted callback phase redirects to reason-only reconciliation; mismatched callback state fails closed. | One-way legacy checkout migration only after owned reservation and payment order/amount verification; name/email are never copied. Retryable verification preserves the exact legacy pair; authoritative mismatch purges it. A target-wins checkout for another accommodation is preserved but never published by the current route | Vercel Preview sandbox cancel/success/fail and OCI confirm/status parity are the remaining pre-merge checks |
| Payment confirmation/status | deleted legacy success/fail routes, confirmation/status hooks, route-state helper, attempt registry, and global payment API | booking-payment callback repository, confirmation reducer/workflow, and feature payment ports | **Active:** exact command single-flight plus terminal lock verifies server ownership, confirms at most once, and reconciles ambiguous/conflict/replay outcomes through exact payment status. Backend confirm/status is the only success authority | A legacy marker is consumed/purged as a reconcile hint; retryable owned checkout/callback documents survive until terminal/session cleanup or TTL | Complete U10; U11 must read the identical U10 schema on rollback |
| Toss runtime | Deleted U10 `tossPaymentsV1.ts`; the v1 runtime is available only through immutable Git commit `408d303` | `src/platform/integrations/tossPaymentsV2.ts` behind unchanged `PaymentGatewayPort`; the confirm route owns its gateway lease | Official npm `2.8.1` is the only active runtime owner and loads `/v2/standard`; the route lease reuses one payment client and destroys its launcher on departure; source/build gates reject direct SDK imports, v1 reachability, and mixed runtime | U10 and U11 use separate commit-specific Vercel deployments and are never mounted together | Record the U11 Preview URL and complete sandbox/OCI parity before merge |
| Review create/images | deleted legacy route/hooks/global review API and DTO | `src/workflows/review-submission/**` | **Active:** one same-Promise create/upload workflow; definitive failures may retry, ambiguous create outcomes terminal-lock, multipart files remain `FormData`, post-create terminals are irreversible, and stale continuations never navigate | Typed `image-upload-failed` result is validated and consumed once at the app boundary; Review Create reads only the minimal owned camelCase reservation model | Complete U9; cache publication failure cannot downgrade a created review and no possible committed create may be blindly repeated |
| Accommodation editor | edit controller plus detail/images/upload/save hooks and refs | listing-editor reducer/commands/controller | current/old | Existing created-draft route state and server detail | U12 proves operation journal/order/stale fences and removes old writer/types cycle |
| Profile guest/host route view | `ProfileRoute.tsx` URL mirrored to local state | Profile codec + controller/screen | current/old | Existing URL query | U13 removes mirrored state and legacy route |
| Guest/host reservation lists | panels/hooks; `useReservationList.ts` infers scope from fetch function identity | Explicit scoped feature Query options and Profile screen | current/old | none | U13 proves distinct keys under wrapped functions and removes identity inference |
| Host listing mutations | `HostListingsPanel.tsx`, accommodation app-shell modal/public cache | host-listing-management workflow and feature ports | current/old | none | U13 switches writer; appShell/publicCache deleted U22 |
| Dialog/Toast/scroll lock | Route components retain only interaction-specific open/message state; non-Dialog custom overlays remain legacy | App OverlayProvider plus current Dialog/Toast public APIs | U19 active: one portal root, ordered/inert stack, topmost dismissal, focus trap/restore, nested scroll lock, Toast timer/unmount cleanup | Dialog and Toast props are unchanged; isolated tests may exercise their inline fallback outside AppProviders | U14 adopts the runtime for all remaining custom overlay consumers; U22 removes fallback/rollback seams after the last compatibility consumer |

## Infrastructure and compatibility ownership

| Surface | Current owner | Target owner | Cutover/removal |
| --- | --- | --- | --- |
| Route definitions/lazy mapping | `src/app/router/**` active; old `src/routes/Router.tsx`/`routeConfig.tsx` deleted; `routeDefinitions`/matching/MainLayout rollback-only | `src/app/router/**` | 15 literal lazy adapters remain; ten bodies are app/screen-owned and five exact body bridges disappear U12-U13/U21; remaining rollback metadata leaves U22 |
| Session/Query runtime | `src/app/session/**` and `src/platform/query/createQueryClient.ts` active; `src/query/QueryProvider.tsx` and `sessionCacheBoundary.ts` compatibility-only | app session plus feature-owned scoped Query options | U5 owns physical generations; Wishlist/Search complete U7/U8; Accommodation Detail/coupons and Reviews complete U9; booking-payment uses captured command scopes U10; remaining slices move U12-U13 and rollback Query surfaces leave U22 |
| Axios/envelope/error | `src/platform/http/**` owns the single client and migrated `AppError`; `src/api/client.ts`, `request.ts`, `response.ts` preserve legacy imports/semantics | feature adapters consume the platform boundary directly | U4 platform owner is active; Auth/Wishlist moved U7, Search U8, Detail/reservation-create/Reviews U9, and booking-payment U10; remaining adapters move U12-U13, global compatibility root leaves U22 |
| Domain API/wire DTO | Auth/Wishlist/Search plus Accommodation Detail/coupons, reservation-create, reviewable-reservation, Reviews, and reservation payment own their adapters/models. Global coupon/review/recently-viewed/payment APIs and their migrated DTOs are deleted; unused `src/types/auth.ts` remains rollback debt | `src/features/*/{api,model,queries}` plus independently enforced `src/features/accommodations/detail/**` and `src/features/reservations/payment/**` scopes | Auth/Wishlist moved U7, Search U8, Detail/reservation-create/Reviews U9, payment U10. Remaining consumers move U12-U13 and global roots/aliases leave U22 |
| Environment | `src/platform/config/**`; `env.ts` is the only app runtime `process.env` reader. The build preflight separately owns CRA's build-only `PUBLIC_URL` asset base. | same platform public-config contract plus explicit build asset-base policy | U4 owner is active; Vite input mapping changes atomically in U16 |
| Google/Daum/Toss globals | `src/platform/integrations/**`; Search consumes the owned Maps loader/runtime and lazy Places adapter directly, while the booking-payment gateway consumes the official Toss v2 adapter through a workflow port | feature ports consume platform integrations | Google/Daum ownership moved U4; Maps/Places facades are removed U8 and the Toss feature facade U10. U11 v2-only runtime cutover is active and awaits Vercel Preview sandbox parity; remaining Daum workflow facade moves U12 |
| Image URL boundary | `src/platform/assets/imageUrl.ts` with legacy `src/utils/image.ts` facade | feature asset ports or direct platform boundary | U4 owner is active; facade consumers move by slice and the facade is removed U22 |
| Shared React test harness | `src/test/renderApp.tsx`, `createTestQueryClient.ts` | same test-only boundary | U4/U19 active; production imports are blocked, tests use the production OverlayProvider, and caller-owned QueryClients/portal roots retain caller lifetime |
| Cross-feature seams | Remaining unmigrated `features/*/appShell.ts`, `publicCache.ts`, and route barrels | app composition and workflows/reconciliation ports | Search, Reviews, Wishlist, Detail, and reservation-payment presentation have no feature-peer imports; app composition joins only public ports and owned cache projections | Other consumers move U12-U13; residual seams leave U22 |
| UI structural runtime | Five app shells and AppHeader are active; remaining screens still contain legacy responsive literals and some custom overlays | app shells/overlays and shared responsive policy | U19 active: sole-main shell contract, app portal/stack runtime, canonical responsive manifest, and JS `matchMedia` policy. CRA-mode gates reject unresolved custom-media consumers; U14 adopts literals across screens and U16 enables transformed aliases |
| Tokens/assets/primitives | `src/styles`, `src/shared/ui`, `src/components`, `src/assets`; `src/components/ListContainer` and `src/components/DatePicker` are compatibility re-exports | `src/shared/{styles,ui,assets}` | U7 moved `ListContainer`; U8 moved the active DatePicker implementation and Search styles onto strict tokens. U15 migrates remaining consumers and U22 removes empty/unused legacy roots and re-exports |
| Build/dev | CRA `react-scripts` after fail-closed validation for API/asset origins, Toss browser keys, build-only `PUBLIC_URL`, and misplaced server-secret shapes | Vite | U16 retains `build/`, env/proxy/assets/chunk parity and the asset-base allowlist; CRA test remains temporarily |
| Unit/integration runner | CRA Jest | Vitest | U17 uses disjoint ownership inventory; removes Jest/react-scripts after last suite |
| Compiler/lint/format | TypeScript 4.9, CRA ESLint presets, dependency-cruiser 17, Knip 2 target ratchet, Stylelint 16 target ratchet | TypeScript 5.x and current explicit static-tool owners | U3 owns graph/reachability/style policy; U23 upgrades Node/TypeScript/tools together after Vitest and performs the final mechanical format pass |

## U10/U11 payment compatibility matrix

| Runtime | Records it may read | Writer | Rollback expectation |
| --- | --- | --- | --- |
| Pre-U10 legacy | Unversioned checkout/index and confirmed marker | Legacy route/hooks | Baseline only; no forward guarantee before U10 migration reader exists |
| U10 with Toss v1 adapter | New owned/versioned checkout and callback records; server-validated legacy one-way input | booking-payment aggregate only | Immutable U10 build must read its own records |
| U11 with Toss npm v2 | Same U10 repository/schema | booking-payment aggregate through v2 gateway only | Local gates plus Vercel Preview sandbox/OCI parity are required before merge |
| U11 rollback to U10 build | U10 records written before rollback | U10 aggregate through v1 gateway | Retryable checkout/callback reconciles through server status; v1/v2 are never active together |

If implementation needs a record shape that breaks this matrix, update the
active plan and browser-data inventory before changing a writer.
